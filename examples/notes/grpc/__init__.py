"""The gRPC transport for the notes app.

There is no ``.proto`` here yet. ``manage.py protos`` writes it, and its stubs,
from the actions declared in ``services.py`` -- which is why the build script
runs that command after copying this app into place.
"""
