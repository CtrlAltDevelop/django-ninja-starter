"""The gRPC transport for the shared infrastructure app.

The generated ``*_pb2.py`` modules live here beside the ``.proto`` that produced
them, which is where django-socio-grpc looks for them. Regenerate with
``python manage.py generateproto``.
"""
